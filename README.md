# couic-docker

This repository contains a [`Dockerfile`](/Dockerfile) for packaging prebuilt binaries of [couic](https://github.com/FCSC-FR/couic) the XDP network filtering solution into a Docker image.
